# Analytics

Owns replayable event ingestion, analytical models, metric definitions, data quality checks, reporting, and analytical jobs. Its data is a projection and must never overwrite transactional facts.
