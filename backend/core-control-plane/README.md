# Core Control Plane

Java/Spring Boot deployment unit for identity and access, channel integration, and core commerce facts.

Create `iam`, `channel-integration`, and `commerce-core` modules here. Each module owns its own schema and publishes versioned events through an Outbox.
