# AIPlatform 简历项目描述库

> 生成日期：2026-07-27  
> 依据：当前仓库的规划、源码、集成验收记录和自动化测试。  
> 使用规则：只能使用你本人实际主导或深度负责的 bullet；将方括号中的真实线上数据补齐后再投递。仓库没有生产 P95、QPS、Recall、成本或用户规模数据，本文绝不虚构这类结果。

## 一句话定位

面向电商运营的多租户 AI 平台：以 Java 控制面承载身份、业务状态、审批与审计，以 Python Runtime/Worker 承载模型推理、RAG 和媒体处理，以 React 工作台统一覆盖内容资产、运营、客服、销售训练、分析和 AI 治理。

## 已验证的项目事实

| 维度 | 可引用事实 |
| --- | --- |
| 业务边界 | 8 个逻辑域：租户与访问控制、渠道接入、电商核心、内容资产、内容运营、智能客服、经营分析、模型与服务治理。 |
| 运行架构 | 5 类部署单元：Java Core Control Plane、Java AI Business Control、Python AI Runtime、Python Media Workers、Analytics；当前统一启动拓扑包含 9 个服务。 |
| 整合范围 | 集成记录纳入 5 类来源能力、837 个文件快照，其中 722 个保持一致、115 个为租户边界/鉴权/降级等适配修改。 |
| 已验证测试 | 集成记录：Java Core 4、Java AI Business 25、Live Clip 11、销售知识 43、Voice 4、内容运营 78、MCP 8、MCP 业务链路 10/10、内容运营 Node 39；主前端 Playwright 19 通过、3 个环境条件用例跳过。 |
| 安全边界 | 浏览器不可信任地传入租户身份；Python Worker 不直写业务表；模型输出不直写高风险业务状态；向量/分析/缓存均是可重建投影。 |

## 推荐简历项目名称

**商媒智能运营平台 / 多租户电商 AI 运营系统**  
技术：Java 21、Spring Boot 3、Spring Security、React 19、TypeScript、Vite、Python/FastAPI、SQLAlchemy、PostgreSQL、Redis、FFmpeg.wasm、FFmpeg、SSE、WebRTC、MCP、pgvector（可选）、Remotion。

## AI 应用开发岗位：大项目版本

**商媒智能运营平台｜AI 应用开发**  
`202X.XX - 202X.XX`｜`个人项目 / 团队项目（按实际填写）`  
技术：Python/FastAPI、Java/Spring Boot、React/TypeScript、PostgreSQL、Redis、SSE、FFmpeg.wasm、MCP、RAG/向量检索（按实际接入状态填写）

面向电商运营的多租户 AI 应用平台，覆盖内容生成、直播切片、知识问答、销售考核、实时语音与模型治理。以“业务证据不可被模型覆盖、高风险操作必须受控、AI 结果必须可追溯”为约束，将 AI 推理与业务状态、人工审核和权限控制解耦。

- 在内容运营、客服、销售训练和媒体处理共享同一业务事实的场景下，设计 8 个逻辑域与 Java 控制面/Python AI Runtime/媒体 Worker 分层；将模型推理、RAG 和媒体计算隔离为可替换执行单元，业务状态、审批和审计仍由受控服务持有，避免 AI 直接写入核心业务数据。
- 为解决多租户 AI 链路中权限上下文易丢失、模型输出难复现的问题，设计可信租户上下文、`ai-task.v1` / `ai-task-result.v1` 异步协议和 AI Trace；关联资产版本、Prompt/模型版本、工具调用、引用、人工审核、Token/媒体/GPU 用量与错误信息，使任一 AI 结果可回溯至输入证据和执行版本。
- 构建知识发布与问答闭环：将原始会话经清洗、人工标注后发布为不可变 Knowledge Release，检索索引作为可重建投影；通过发布版本和租户范围约束 RAG 输入，并在依赖未连接时明确降级，避免返回编造的知识或成功状态。
- 针对长视频处理的带宽与成本约束，实现浏览器端 FFmpeg.wasm 抽取 16kHz 单声道音频、服务端 ASR 与 LLM 片段规划、浏览器本地按时间码导出 MP4/ZIP 的链路；以异步状态机、失败重试、SSE 进度和 JSON/时间范围校验处理长耗时与不确定模型输出。
- 设计内容生成与销售考核的人机协同流程：内容从 Brief 到草稿、审核、发布意图均保留不可变版本；AI 评分仅产生建议，最终成绩由冻结量表与人工复核确定，降低生成模型在发布和人员考核中的误用风险。
- 建立模型网关、Prompt/Tool 版本、预算和配额控制的统一入口，使用工具白名单、参数 Schema、输出过滤和业务侧二次鉴权约束 MCP/Agent 工具调用；支持模型与 Prompt 的评估、灰度和回滚，而不是将模型调用散落在各业务模块。
- 完成跨 Java、Python、Node 与 React 的回归验证：集成记录包含 Java 控制面 29 项、Python Runtime/Worker 144 项、MCP 18 项、内容前端 39 项及主前端 Playwright 19 项通过；上线后可补充真实的 `[评测集规模]`、`[Recall@K]`、`[P95]`、`[任务成功率]` 和 `[单次成本]` 作为结果指标。

**投递前替换项**：将时间、角色、个人负责范围，以及最后一条中的真实业务数据补齐；若尚未接入生产向量库、RTC 或模型供应商，保留“接口抽象/显式降级”的表述，不写为生产能力。

## AI 应用开发全流程 Bullet 库

> 此节故意不压缩。它覆盖从问题定义到交付治理的完整 AI 应用工程链路；投递时保留与你个人职责和目标岗位最匹配的 6-10 条。没有真实生产数据的条目只陈述可由代码和测试验证的机制，不虚构业务结果。

### 1. 问题建模与系统边界

- 在电商内容、客服、销售训练和媒体处理共用商品、资产与用户证据的场景下，将“模型给出候选”与“业务系统作出最终决定”分离；以业务事实、原始证据、AI 派生结果、人工审核和最终执行构成单向闭环，避免生成结果覆盖原始数据。
- 从多租户 AI 应用的最小信任边界出发，规定租户、用户、店铺范围和调用用途必须由控制面注入，不能由浏览器或模型参数声明；将跨租户访问、越权检索和异步任务上下文丢失作为首要失败场景设计。
- 按数据所有权而非“一个功能一个微服务”划分 8 个逻辑域，将身份/业务状态/审批审计与推理/RAG/媒体计算分离为 Java 控制面、Python AI Runtime 和媒体 Worker，既避免共享写表，也保留模型和基础设施的替换空间。
- 为每个跨服务请求定义 `trace_id`、可信 `tenant_id`、主体、店铺范围、用途、事件 ID、聚合版本和 Schema 版本等上下文字段，为追踪、幂等、对账和权限复核提供共同语言。

### 2. 数据、资产与知识工程

- 将原始聊天、文档、图片、音视频等输入统一视作带版本、来源、版权、保留策略和授权范围的资产；解析文本、OCR、ASR、关键帧、字幕和剪辑片段均作为可追溯的派生产物，记录父资产、处理参数、算法/模型版本与任务信息。
- 构建 `RawChat -> StagingConversation -> LabeledConversation -> KnowledgeArticle/TrainingExport` 的单向数据链：原始会话保持不可变，清洗候选可重算，人工标注后才可发布为知识或训练投影，防止人工编辑和训练导出反向污染证据。
- 实现会话 ETL 的 upsert、时间/噪声过滤、连续消息合并和会话分组，并为重复导入、搜索过滤、文本 staging、Schema 同步等数据质量风险编写回归测试。
- 将知识可用性建模为不可变 Knowledge Release，而非“当前知识库”；线上问答固定关联某一发布版本/别名，支持历史复现、回滚和增量重建，避免索引更新导致同一问题答案不可解释地漂移。
- 把向量索引、全文索引和分析表定位为可从发布知识和权威业务数据重建的投影；当向量库、对象存储或数据库未连接时返回明确的 `DEPENDENCY_NOT_CONNECTED` 等状态，不以空数据伪装成功。
- 面向 PDF、Office、网页快照、图片与音视频设计解析边界：保留页码/坐标/时间戳等证据位置，使 RAG 引用、人工质检和后续素材复用可定位回源文件。

### 3. 检索与 RAG 应用

- 构建关键词/向量检索的可插拔适配层，使 MVP 可先以全文/关键词检索闭环，再在评测数据证明瓶颈后接入 pgvector 或专用向量库，避免在未验证召回收益前引入不可控存储复杂度。
- 为 RAG 查询施加租户、店铺、知识发布版本和资源授权的多层过滤，并要求回答关联证据引用；将“召回正确”与“模型生成流畅”分开验证，降低跨租户泄露和无依据回答风险。
- 为客服知识问答设计“证据召回 -> 带引用答案候选 -> 低风险自动答复/高风险人工确认 -> 无法解决转工单”的路径，将转人工原因、人工纠错和错误答案沉淀为后续评测集输入。
- 设计知识发布、索引构建和客服会话之间的版本关系，使索引可以蓝绿重建和回滚，而会话仍能复现当时使用的知识版本与引用结果。
- 将 RAG、Memory 和 MCP 设计为端口而非强耦合存储实现，保留替换检索器、会话记忆后端和工具协议的能力，避免领域业务被某一向量数据库或 Agent 框架锁定。

### 4. LLM 编排、Prompt 与结构化输出

- 建立统一模型网关，集中处理 Provider/Deployment 路由、模型能力、超时、重试、熔断、并发和限速；业务服务只声明任务质量、数据合规、上下文长度、时延和预算约束，不直接绑定具体供应商。
- 将 Prompt、结构化输出 Schema、Tool Schema、工作流和安全策略版本化；支持审核、灰度、回滚与历史复现，使 Prompt 修改具备与代码发布相近的可控性。
- 对片段规划、标题、编辑指南、内容草稿、试题和评分建议等 LLM 输出使用 JSON Schema、枚举/范围校验和业务规则二次验证；例如校验视频时间码不越界、不重叠且符合目标时长，阻断格式正确但语义非法的输出。
- 对不可确定的 Provider 超时设计“请求 ID + 状态查询 + 不确定状态”而非盲目重试，避免上游已成功但响应丢失时产生重复生成、重复计费或重复外部动作。
- 将多步骤工作流设计为显式状态和输入/输出契约，而非把业务状态藏在 Agent 内存中；只有存在分支、循环、人工中断与可恢复状态时才引入工作流引擎，单次结构化生成保持简单调用链。
- 将内容生成落入 Brief、选题、草稿、内容版本、审核、日历、媒体意图、平台变体和发布意图等业务对象，模型负责产生候选而不是隐式完成“发布”动作。

### 5. Tool / MCP 与 Agent 安全

- 为模型工具调用建立 Tool Catalog 和统一 HTTP/MCP Gateway，将发现能力、调用参数和调用策略标准化；HTTP 网关负责鉴权、租户范围、预算和审计，避免客户端或 Agent 绕过控制面直连工具。
- 以工具白名单、参数 Schema、输入输出过滤、PII 脱敏和业务侧二次鉴权防御提示词注入；模型即使生成越权调用，也不能直接执行发布、退款、工单结案或人员考核等写操作。
- 将写工具设计为“提交请求/计划”语义，最终执行方重新校验当前状态、权限和幂等键；读工具仅暴露完成当前任务所需的最小字段，降低上下文泄露和误操作影响面。
- 将模型密钥、Provider 密钥和服务身份与业务配置、日志和前端显示隔离；UI 仅支持一次性展示新建密钥，运行时配置通过受控环境或密钥管理注入。

### 6. 异步 AI 与媒体任务工程

- 将 ASR、OCR、视频转码、批量生成、索引构建和评测等长耗时操作任务化，使用 `pending -> processing -> completed/failed -> retry` 状态机、进度、取消、超时、失败分类和人工补偿，避免同步接口被长任务占用。
- 在直播切片中采用浏览器 FFmpeg.wasm 提取 16kHz 单声道音频、服务端执行 ASR 和 LLM 片段规划、浏览器对本地原视频导出 MP4/ZIP 的分工，以较低上传带宽和服务端存储换取浏览器需保留源视频的约束。
- 通过 SSE 向前端推送任务进度，并保留轮询兜底；客户端断开不影响服务端任务，任务重试、删除和结果读取均围绕持久化任务 ID 而非页面内存状态。
- 为 Worker 设计 lease、幂等键、attempt、阶段 checkpoint 和结果唯一约束，防止多实例重复领取、重启后粗粒度重跑和重复计费；Worker 只回传进度、产物、成本和错误，不直接写业务表。
- 将媒体产物与生成任务关联到父资产版本、处理配置、算法/模型版本及版权审核状态；未经授权或审核的素材不能自动进入知识库、内容生成或渠道发布。

### 7. 人机协同与高风险决策

- 对内容发布、售后、工单结案和销售考核等高风险动作采用“AI 候选 + 人工审核/确定性策略 + 最终执行”模型；模型结果作为建议而非事实，显式保留审核人、理由和最终状态。
- 设计销售考核闭环：从冻结知识版本生成试题候选，人工审题后发布 QuizVersion，AI 仅按冻结量表给出逐题建议，人工复核后写入 FinalGrade，兼顾自动化与公平性。
- 为实时语音客服实现 session 与 turn 的分离：打断只终止当前回答而不关闭会话；无法依据证据回答、外部依赖不可用或风险升高时转人工/工单，不将“继续生成”当作默认降级。
- 将人工修改、拒绝、转人工原因、质检标签和最终业务结果回流到评测/分析边界，为 Prompt、检索和流程改进提供可审计的反馈数据，而非只记录模型文本。

### 8. 评测、质量与回归

- 设计脱敏评测集、标准答案、自动指标、人工评审、对抗样例与回归门禁的模型发布流程；新模型、Prompt 或检索配置未通过核心评测与安全样例时不得全量发布。
- 将“召回质量、引用正确性、结构化输出成功率、人工采纳率、转人工率、任务成功率、P95、单次成本”定义为不同层级指标，避免只用主观模型评分代替业务质量。
- 覆盖控制面、Runtime Gateway、媒体/知识/语音 Worker、内容运营前端、MCP Agent 和主前端 E2E 的跨语言回归；当前集成记录显示 Java 控制面 29 项、Python Runtime/Worker 144 项、MCP 18 项、内容前端 39 项与主前端 Playwright 19 项通过。
- 将依赖未接入、Provider 超时、非法模型 JSON、Worker 重复消费、跨租户检索、回调重放和浏览器资源不足纳入失败测试，而不是只验证“模型正常返回”的 happy path。

### 9. 可观测性、成本与可靠性

- 为每次模型调用记录请求、上下文引用、Prompt/模型/工具版本、响应、错误、延迟、Token/媒体/GPU 用量和成本，形成端到端 AI Trace；敏感正文按策略脱敏、加密或仅保留哈希引用。
- 将预算预留与结算、软提醒、硬拦截、并发/速率限制按租户、项目、任务和模型维度拆分，避免一个租户的突发流量或预算耗尽挤占其他租户的保底资源。
- 设计 Provider 故障降级：切换备用模型、缩短上下文、返回检索模板、转人工或延迟队列；区分幂等与非幂等任务的重试策略，避免故障恢复引发重复副作用。
- 将业务操作、消息、模型调用和媒体任务统一关联 `trace_id`，使“用户操作 -> AI 候选 -> 审核 -> 执行/失败 -> 成本”可跨 Java/Python/浏览器追踪。

### 10. 交付、集成与前端体验

- 将 5 类既有 AI 能力收敛进统一 React 工作台，提供 8 个域的登录保护、租户切换、依赖状态展示和响应式入口；避免多套独立前端各自保存认证、服务地址和状态模型。
- 抽象共享 HTTP 客户端，统一 Bearer Token、租户 ID、Trace ID、JSON/Blob/SSE 处理与错误模型；页面只表达业务请求，减少认证与下载/流式细节的复制。
- 使用统一启动/停止脚本管理 9 个服务和健康检查，并以集成快照验证迁入内容、构建产物、运行端口和密钥扫描；将真实密钥、数据库转储、录音和生成媒体排除在版本库之外。
- 以“可用本地操作、待执行的模拟任务、未连接的外部依赖”三种 UI 状态明确呈现系统能力，避免 Demo 用伪造的增长数字、同步状态或模型结果误导使用者。

## AI Agent 工程化：生产级大项目表述

> 这一版适合 AI 应用开发/Agent 工程师岗位。核心不是“调用过大模型”，而是把不确定的模型能力放进可验证、可恢复、可限制、可追溯的业务系统。以下表述均源于当前项目的架构、代码或集成验证；“生产级”指工程机制和故障模型按生产要求设计，不能替代真实线上 SLA、调用量或成本指标。

**商媒智能运营平台｜生产级 AI Agent 应用工程**  
`202X.XX - 202X.XX`｜`个人项目 / 团队项目（按实际填写）`  
技术：Python/FastAPI、Java/Spring Boot、React/TypeScript、PostgreSQL、Redis、SSE、FFmpeg.wasm/FFmpeg、MCP、RAG、SQLAlchemy、WebRTC（按实际接入状态填写）

### 从第一性原理定义 Agent

- 将 Agent 定义为“在受限状态空间内生成候选计划并调用受控工具”的执行单元，而非拥有数据库和无限权限的聊天机器人；业务事实、权限判断、审批和最终写操作仍由确定性的控制面负责。
- 从模型输出天然不确定、上下文天然不完整、工具副作用不可逆三个约束出发，采用“检索证据 -> 模型候选 -> Schema/规则校验 -> 人工或策略审批 -> 业务系统执行”的闭环，避免 LLM 直接完成发布、退款、工单结案和人员考核。
- 将复杂 Agent 工作流显式建模为状态、节点、输入/输出契约和可恢复检查点；仅在确有分支、循环、人工中断和多步状态时使用工作流编排，单次结构化生成保持为简单且可测试的调用链。
- 将 Agent 的短期状态、领域业务状态和长期知识拆分：会话/工作流状态不替代业务表，知识索引不替代原始资产，模型记忆不替代权限系统，避免状态漂移导致不可重放或越权决策。

### Agent 任务与可靠性工程

- 将 ASR、RAG 构建、批量生成、评测、视频渲染和 Agent 多步任务全部任务化，使用 `pending -> processing -> completed/failed -> retry` 状态机管理进度、取消、超时、失败分类和人工补偿，避免 HTTP 请求超时后任务状态丢失。
- 为跨 Java/Python 的 Agent 任务设计 `ai-task.v1` / `ai-task-result.v1` 契约，控制面创建、鉴权并持久化业务任务，Worker 只消费受控输入并回传产物、进度、成本和错误；禁止 Worker 直接写业务表，避免 AI Runtime 成为隐式主数据源。
- 使用幂等键、lease、attempt、阶段 checkpoint 和结果唯一约束解决 Worker 多实例重复领取、进程重启重跑、消息至少一次投递和 Provider 超时后实际成功等问题；对不确定的外部调用保存 request ID 并查询最终状态，而不是无条件重试。
- 通过 SSE 输出可观察的任务进度并保留轮询兜底，页面断开不取消后台任务；以持久化 task ID 而非浏览器内存协调重试、删除、查询和结果导出，提升长任务在刷新、断网和多端访问下的可恢复性。
- 将 Agent 失败拆分为输入校验、依赖未连接、Provider 限流/超时、结构化输出无效、业务规则拒绝和人工拒绝等类别，为重试、降级和运营定位提供不同策略，而不是以统一异常吞掉问题。

### RAG 与知识可信性工程

- 将“回答正确”拆解为知识是否可用、是否被正确召回、引用是否充分、生成是否遵循证据四个独立问题；以不可变 Knowledge Release 固定线上可检索语料，防止文档编辑或索引更新使历史会话不可复现。
- 构建原始会话到知识发布的证据链：`RawChat -> StagingConversation -> LabeledConversation -> KnowledgeArticle/TrainingExport`；原始数据不可变、候选可重算、人工标注后才发布，使模型回答和训练样本均可定位到来源版本。
- 为检索请求施加 tenant、店铺、知识发布版本、资源授权和用途过滤；将向量/全文索引作为可重建投影，并以跨租户检索拒绝测试防止“召回正确但拿到别的租户数据”的安全事故。
- 将 RAG 结果连同 chunk/资产引用、检索配置、Embedding/索引版本、Prompt 和模型版本写入 AI Trace；使一次错误回答能够区分是语料缺失、检索失效、上下文截断还是模型幻觉。
- 采用端口化检索适配层，允许先以关键词/全文检索完成闭环，再依据冻结评测集的 Recall@K、引用正确率和 P95 决定是否迁移 pgvector 或专用向量库，避免技术选型先于问题验证。
- 当向量库、对象存储、Embedding 或模型依赖未接入时显式返回依赖不可用；不伪造索引成功、召回结果或模型回答，使上层 Agent 能选择转人工、延迟队列或仅提供确定性功能。

### 工具调用、MCP 与安全控制

- 为 Agent 建立 Tool Catalog、Tool Schema 和统一 HTTP/MCP Gateway：模型只从白名单发现工具，网关集中执行身份校验、租户范围、预算、限流、审计和错误规范，避免 Agent 直连内部服务或第三方 API。
- 将有副作用的工具改为“提出计划/提交请求”语义，最终业务执行方再次校验当前权限、资源状态、审批和幂等键；通过双重鉴权避免模型在旧上下文或提示词注入下执行过期/越权操作。
- 对工具参数实施 JSON Schema、枚举、范围、资源归属和业务状态校验；对工具结果进行最小字段返回、敏感信息脱敏与输出过滤，防止模型从读工具获得不必要 PII 或将不可信文本带入后续 Prompt。
- 使用 Tool allowlist、Prompt 注入检测、输入输出内容安全过滤和业务侧授权作为纵深防御；即使恶意知识文本诱导模型提升权限，也不能突破网关和业务服务的确定性校验。
- 将工具调用关联到 Agent 任务、调用主体、tenant、用途、参数摘要、返回摘要、耗时和成本，形成可审计链路；支持根据 Trace 定位“模型决定、工具失败、业务拒绝”分别发生在哪一层。

### Prompt、模型路由与结构化输出

- 建设统一模型网关管理 Provider、Deployment、能力版本、密钥引用、超时、重试、熔断、并发和速率限制；业务 Agent 只表达质量等级、上下文长度、合规、时延和预算约束，从而支持供应商替换和模型降级。
- 对 Prompt、系统策略、输出 Schema、工具定义和工作流版本进行登记、审核、灰度、回滚和历史复现；把 Prompt 视为可发布工程资产，而不是散落在业务代码里的字符串。
- 对 Agent 输出实施“解析 -> Schema 校验 -> 业务语义校验 -> 修复/重试 -> 人工兜底”管道；例如视频片段必须通过时间边界、重叠、时长和输入资产版本校验，内容发布候选必须经过审核状态机。
- 为模型路由设置质量、预算、上下文、可用性和数据合规约束，并在故障时支持备用模型、上下文压缩、检索模板、延迟队列和转人工；区分幂等生成与非幂等外部动作的重试边界。
- 将 Token、媒体/GPU 用量、模型响应延迟、结构化输出成功率和失败原因归集到任务/租户/模型维度，为后续以真实数据优化路由、缓存与预算提供基础，而不是只统计“调用次数”。

### 多模态 Agent 与系统性能优化

- 针对长视频直接上传造成的带宽、存储和服务端转码压力，设计浏览器侧 FFmpeg.wasm 先提取 16kHz 单声道音频，服务端只做 ASR/片段规划，最终在浏览器按时间码导出 MP4/ZIP；以客户端计算换取更低网络与服务端负载，并保留服务端 FFmpeg 导出作为能力降级。
- 将音视频、OCR、视觉解析、批量图像生成和 Remotion 渲染隔离到异步 Worker，按 CPU/GPU 和长任务特征独立扩缩容，避免模型/转码负载侵占 Java 控制面处理认证、审批和查询的资源。
- 将大文件输入改为受控资产版本引用而非客户端本地路径；输入经 MIME、大小、哈希去重、版权/保留策略和授权检查后进入任务，避免路径穿透、跨用户引用和无效重复计算。
- 通过任务阶段化、进度回传与中间产物持久化支持断点恢复；将转写、关键帧、字幕和片段规划记录为可复用派生产物，避免同一资产在不同 Agent 流程中重复执行高成本计算。
- 对浏览器本地导出明确检查文件大小、时长、内存和取消条件，并在资源不足时提示/切换服务端路径；将客户端资源耗尽视为一等失败模式，而不是把失败归为“模型异常”。

### 人机协同、评测与持续优化

- 为内容生成、客服答复、销售考核和发布动作设计分层自治：低风险、证据充分的候选可走确定性策略；高风险、低置信或依赖缺失的任务转人工/工单，并保存转人工原因和最终纠错结果。
- 设计销售考核的冻结量表与人工终审机制：AI 仅输出逐题建议和理由，不直接覆盖最终成绩；人工修改保留原因，使质量、偏差和申诉可以回流评测，而非被一次模型调用永久固化。
- 将错误答案、人工拒绝、转人工原因、工具失败和用户反馈沉淀为脱敏评测集；以自动指标、人工评审、对抗样例和回归门禁评估新 Prompt、模型与检索配置，阻止未验证变更全量发布。
- 将质量指标拆解为 Recall@K、引用正确率、回答采纳率、结构化输出成功率、转人工率、任务成功率、P95、成本和预算拦截率；每项指标对应数据采集点和责任层，避免单一主观“模型评分”掩盖链路问题。
- 当前仓库已对控制面、Runtime Gateway、媒体/知识/语音 Worker、MCP Agent、内容前端与主前端进行跨语言回归，集成记录包含 Java 控制面 29 项、Python Runtime/Worker 144 项、MCP 18 项、内容前端 39 项、主前端 Playwright 19 项通过；投递前应以真实监控数据替换为评测规模、线上 P95、成功率和单位成本。

### 可观测性、成本与生产运行

- 构建端到端 AI Trace，关联用户操作、任务、资产/知识版本、检索引用、Prompt、模型、工具、审核、最终业务结果、延迟和成本；通过统一 `trace_id` 跨浏览器、Java、Python 和 Worker 排障。
- 对日志采用字段白名单、脱敏、加密或哈希引用策略，避免将完整 Prompt、正文、Token 和密钥写入日志；同时保留足以定位模型版本、输入版本和失败阶段的最小诊断信息。
- 按租户、项目、任务和模型执行预算预留/结算、软提醒、硬拦截、并发和速率控制，解决单租户突发调用耗尽共享模型配额的问题；预算耗尽时按任务类型选择拒绝、降级或排队。
- 将 Provider 故障、依赖未连接、Worker 崩溃、索引版本切换、回调重放和浏览器资源不足纳入可观测的运行状态；以备用模型、重建投影、延迟队列、人工接管和回滚构成降级策略。
- 用统一启动/健康检查管理 9 个协同服务，并通过集成快照、构建、密钥扫描和跨端 E2E 验证交付；将数据库转储、真实密钥、录音和生成媒体排除出代码库，避免开发集成泄露生产数据。

### 面试使用方式

- 简历正文选择其中 8-12 条：优先保留“Agent 边界/任务可靠性、RAG 可信性、工具安全、结构化输出、评测与 Trace”六类内容，再按岗位补充多模态或性能优化。
- 面试中每条都按“故障模式 -> 为什么常规做法会失败 -> 机制与取舍 -> 验证方式 -> 真实指标或待补指标”展开；例如不要只说“用了 RAG”，而要说明版本发布、租户过滤、引用、索引重建和评测分别解决什么问题。
- 只陈述本人实际参与的设计与实现。尚未接入生产 Provider/向量库/RTC 的部分，表述为“接口与降级机制已实现”，不要表述成“线上稳定运行”。

## 最终推荐：AI 应用开发岗位项目描述

**商媒智能运营平台｜企业级 AI Agent 应用工程**  
`202X.XX - 202X.XX`｜`个人项目 / 团队项目（按实际填写）`  
技术：Python/FastAPI、Java/Spring Boot、React/TypeScript、PostgreSQL、Redis、SSE、MCP、RAG、FFmpeg.wasm/FFmpeg、SQLAlchemy

面向电商内容运营、智能客服、销售训练与多模态资产处理，构建多租户 AI Agent 平台。核心目标是在模型输出不确定、知识持续变化、工具存在副作用和长任务易失败的约束下，让 AI 能力具备企业级的权限边界、可追溯性、可靠性和人机协同闭环。

- 在内容生成、客服问答、销售考核和媒体处理共享商品/资产/用户事实的约束下，按数据所有权拆分 8 个逻辑域，并采用 Java 控制面 + Python AI Runtime/Worker 分层；控制面持有身份、业务状态、审批和审计，Agent 仅生成候选或执行受控计算，避免模型直接写入核心业务表。
- 针对 Agent 多步骤执行中刷新页面、进程重启、多实例竞争和 Provider 超时可能造成状态丢失或重复副作用的问题，设计 `ai-task.v1` / `ai-task-result.v1` 跨服务任务协议及 `pending -> processing -> completed/failed -> retry` 状态机；结合幂等键、lease、attempt、checkpoint、结果唯一约束和错误分类实现可恢复的异步执行。
- 构建版本化 RAG 知识闭环：将原始会话按 `RawChat -> StagingConversation -> LabeledConversation -> Knowledge Release` 演进，保留不可变源证据与人工标注；检索侧强制 tenant、店铺、发布版本和资源授权过滤，索引作为可重建投影，解决知识更新后历史回答不可复现及跨租户召回泄露问题。
- 为 Agent 工具调用建立 Tool Catalog、Schema 和统一 HTTP/MCP Gateway；模型只能调用白名单工具，网关执行认证、租户范围、预算、限流、审计和参数校验，写操作采用“生成计划/提交请求 -> 业务系统二次鉴权、状态校验与审批”的两阶段机制，防止提示词注入和陈旧上下文导致越权执行。
- 建立 Prompt、模型部署、输出 Schema、工具定义和工作流的版本化治理；对 LLM 输出执行“解析 -> JSON Schema 校验 -> 业务语义校验 -> 修复/重试 -> 人工兜底”管道，校验视频时间码、内容审核状态和考核评分边界，避免格式合法但业务非法的结果进入下游。
- 针对长视频直接上传带来的网络与服务端计算压力，实现浏览器 FFmpeg.wasm 提取 16kHz 单声道音频、服务端 ASR/LLM 生成切片规划、浏览器按时间码导出 MP4/ZIP 的分工；通过异步 Worker、SSE 进度、轮询兜底和服务端导出降级，在低带宽与长耗时约束下保障可用性。
- 建设端到端 AI Trace，关联调用主体、tenant、资产/知识版本、检索引用、Prompt/模型/工具版本、审核结果、延迟、Token/媒体/GPU 用量和错误；按租户、任务和模型执行预算预留/结算、配额、并发与速率控制，支持备用模型、上下文压缩、延迟队列和人工接管等故障降级。
- 将内容发布、客服答复和销售考核设计为人机协同而非全自动决策：AI 生成带证据的候选，低风险场景由确定性策略执行，高风险或低置信任务进入人工审核/工单；人工拒绝、修改和转人工原因沉淀为脱敏评测数据，驱动 Prompt、检索和流程回归。
- 建立覆盖模型质量、安全和可靠性的交付门禁：以评测集、引用正确性、结构化输出成功率、任务成功率、转人工率、P95 和单位成本作为独立指标；集成验证覆盖 Java 控制面 29 项、Python Runtime/Worker 144 项、MCP 18 项、内容前端 39 项和主前端 Playwright 19 项回归，确保跨语言服务链路可构建、可验证、可降级。

**真实指标补齐后替换最后一条或追加一条**：在 `[评测集规模]` 条冻结样本上，将 Recall@K 从 `[X]` 提升至 `[Y]`，引用正确率达到 `[X%]`；将 Agent 任务成功率提升至 `[X%]`，P95 控制在 `[X 秒]`，单次任务成本控制在 `[X 元]`。仅填写可由评测报告、监控或账单复核的数据。

## 平台级候选 Bullet

- 在内容生产、客服、销售训练和媒体处理共存的电商运营场景下，按数据所有权而非技术栈拆分 8 个逻辑域与 5 类部署单元；将权威业务状态留在 Java 控制面，将推理、RAG 和媒体计算隔离到 Python Runtime/Worker，避免 AI 服务直写业务表并降低跨域耦合。
- 为解决多租户 AI 链路中“调用方伪造租户、异步任务丢失授权上下文、检索投影越权”的风险，设计租户/店铺/主体/用途的可信上下文注入和受控 API 边界；规定任务通过 `ai-task.v1` / `ai-task-result.v1` 协议交接，Worker 仅回传结果、进度、成本和失败分类。
- 建立从资产版本、Prompt/模型版本、工具调用、人工审核到最终业务结果的可追溯链路；将向量索引、分析表和缓存定义为可从权威数据重建的投影，避免 AI 输出反向覆盖原始证据。
- 将 5 类已有 AI 能力收敛到统一平台，而非复制为独立产品；完成 837 个受管文件的集成快照与安全适配，并以 Java、Python、Node 和 Playwright 覆盖的自动化回归验证跨服务主链路。
- 为统一前端实现认证保护、租户切换、8 个工作区导航及共享 HTTP 管道；业务请求自动注入 Bearer Token、租户 ID 和 Trace ID，统一处理 JSON、下载和 SSE 响应，减少页面对服务地址和认证细节的耦合。

## 直播切片与媒体资产处理

**适用定位：内容资产平台 / 多模态媒体处理 / AI 剪辑 Agent**

- 在长视频上传带宽、服务端存储和浏览器导出体验互相冲突的约束下，设计“浏览器 FFmpeg.wasm 抽取 16kHz 单声道音频，服务端仅执行 ASR 与片段规划，浏览器按时间码导出 MP4/ZIP”的链路，避免原始视频重复上传；代价是浏览器关闭后不能由服务端独立产出最终视频，保留服务端 FFmpeg 作为降级路径。
- 将直播切片拆成可恢复的异步状态机 `pending -> processing -> completed/failed -> retry`，提供任务删除、失败重试、SSE 进度订阅和轮询兜底；通过任务/片段持久化隔离长耗时计算与 HTTP 请求生命周期。
- 组合时间戳 ASR、LLM 片段规划、爆款标题与剪辑指南生成，输出片段起止时间、评分、理由、字幕和编辑建议；对模型生成的 JSON 增加 Schema、时间范围、重叠和时长校验，避免非法片段直接进入导出。
- 从“任意 `source_path` 可被请求引用”的风险出发，改为由受控资产版本 ID 解析输入，规划以租约、幂等键、阶段 checkpoint 和 attempt 记录替代进程内领任务锁，支撑多实例扩展与重复投递防护。
- 为媒体 Worker 定义“进度、产物、成本、失败分类”回传契约，并在集成验收中通过 11 个 Live Clip Python 用例及桌面端真实浏览器音频提取/ZIP 导出链路验证。

## 销售知识库、数据治理与 AI 考核

**适用定位：企业知识库 / RAG 数据工程 / 销售培训平台**

- 面向微信聊天 SQLite、素材、报告和人工标注混杂的数据源，设计 `RawChat -> StagingConversation -> LabeledConversation -> KnowledgeArticle/TrainingExport` 的单向证据链；将原始会话设为不可变证据，允许候选清洗与知识/训练投影重算，避免标注结果回写污染原始数据。
- 实现微信会话 ETL、时间/噪声过滤、连续消息合并、会话分组、人工编辑/通过/拒绝和知识发布流程；通过 upsert 与幂等测试防止重复导入，覆盖数据接入到知识发布的可复核闭环。
- 构建关键词/向量检索、RAG 问答、素材与报告管理、图像/文档解析、脱敏、学生管理和训练数据导出能力；检索存储以接口抽象，可在未连接向量库时显式降级而非伪造命中结果。
- 设计销售测评的“知识版本选题 -> AI 出题候选 -> 人工审题发布 -> 学员作答 -> AI 建议评分 -> 人工复核 -> FinalGrade”流程，将 AI 评分限制为候选意见，冻结量表并保留人工调整理由，避免模型直接决定人员考核。
- 围绕数据质量、权限与可维护性补齐 ETL 幂等、搜索过滤、文本 staging、Schema 同步、学生-报告绑定和素材目录迁移测试；集成记录显示销售知识 Python 43 项测试通过。

## 实时语音客服与人工接管

**适用定位：实时语音 AI / 客服系统 / RTC 工程**

- 设计租户级 VoiceSession 生命周期：用户同意录音/转写后由控制面创建会话，RTC Adapter 签发短期房间 Token，客户端进入房间后以幂等 VoiceTask 驱动 ASR、RAG/LLM 流式回答、TTS 和字幕回传。
- 将“打断”定义为终止当前 turn 而非结束 session，并在离房时收敛 Stop 与状态；录音、转写和摘要遵循显式保留/删除策略，无法解答时转人工与工单，避免高风险场景的自动闭环。
- 针对 RTC 回调重放和串话风险设计签名、nonce、时间窗及 session/turn 校验；在外部 RTC、向量库或模型配置缺失时返回明确依赖不可用状态，前端展示人工接管降级路径。
- 通过 Voice Runtime 的安全测试与端到端回归覆盖授权登记、依赖缺失失败和响应式语音工作区；不将未配置的真实麦克风/RTC 链路表述为生产可用。

## AI 内容运营与营销自动化

**适用定位：AIGC 工作流 / 内容运营平台 / 人机协同审核**

- 将内容运营从单次文本生成拆成 Brief、选题、草稿、内容版本、人工审核、运营日历、媒体意图、平台变体和发布意图等可审计对象；内容版本保持不可变，发布必须经人工审核或确定性规则，而非直接信任模型输出。
- 为选题规划、品牌语气、文案撰写、改写、海报、视频脚本、平台适配和分发设计独立工作台与状态流；模型推理由 Python Runtime 负责，Java 控制面保存任务、审批和审计状态，降低模型供应商变更对业务状态的影响。
- 对长耗时生成使用任务化提交、流式状态返回和失败分类；在 Provider 未配置时显式降级为演示候选或依赖未连接状态，避免界面伪造“已生成/已发布”。
- 将图像、脚本、音频段和 Remotion 渲染请求建模为可追溯的派生产物，记录输入资产、处理参数和版本；使视频渲染可以作为独立 Worker 横向扩展，不占用业务 API 的请求线程。
- 已有测试覆盖内容运营 Python 78 项、旧前端 Node 39 项，以及主前端“简报-选题-草稿-人工审核”桌面/移动回归；可在补齐真实数据后填写“审核吞吐、生成耗时、采纳率、发布成功率”。

## MCP 共享 AI 服务与模型治理

**适用定位：LLM Gateway / AI 平台工程 / MCP 服务治理**

- 为多业务线重复保存模型密钥、Prompt、重试和日志的问题，设计统一模型网关，集中管理 Provider/Deployment 路由、超时、重试、熔断、限流、预算与 Trace；业务域仍持有发布、退款、工单等最终决策。
- 建立 Prompt Hub、Tool Catalog、Memory、RAG 和 LLM Gateway 的端口化共享服务；通过 MCP 将工具发现和调用标准化，同时保留 HTTP Gateway 作为外部策略、鉴权、租户范围和配额的强制入口。
- 将模型调用与 Prompt、工具、引用、Token/媒体/GPU 用量、延迟、错误和成本关联为 AI Trace；支持模型/Prompt 评估、灰度和回滚，避免升级后无法复现历史结果。
- 对提示词注入和越权工具调用实施 allowlist、参数 Schema、输出过滤和业务侧二次鉴权；对 RAG 结果使用发布版本与租户双重过滤，确保索引是可重建投影而不是权威数据源。
- 集成验收中 MCP 租户 Agent 8 项及原始网关业务链路 10/10 通过；仓库没有真实模型调用成本、可用性和 P95 数据，不应写成生产 SLA。

## Java 控制面：身份、业务、审批与审计

**适用定位：多租户 SaaS 后端 / 控制面架构**

- 在一个自然人可跨多个租户、同一租户角色和店铺范围不同的场景下，实现认证、注册、会话恢复、租户切换和受保护路由；将租户 ID 从可信登录上下文取得，而非由浏览器请求体声明。
- 将 Core Control Plane 与 AI Business Control 按权威数据边界拆分：前者负责身份、租户和电商基础事实，后者负责资产、内容、知识发布、会话、工单、考核、审批和审计；各模块独立拥有 Schema，并通过版本化 Outbox 事件协作。
- 设计由 Java 创建并校验 `ai-task.v1` / `ai-task-result.v1` 的任务边界，禁止 Python 直接写入业务表；在 Runtime Gateway 中通过受控 HTTP 调用转发到直播切片、内容运营、语音、销售知识和共享 AI 服务。
- 在 API 层建立业务请求的租户认证、`X-Tenant-Id`、`X-Trace-Id` 贯穿与统一错误处理；支持 JSON、二进制下载与 SSE 流式响应，且不让各页面重复实现认证与追踪逻辑。
- 集成记录中 Java Core 4 项、Java AI Business 25 项测试通过，包含控制面 API 与各 Runtime HTTP Gateway；适合补写“鉴权拒绝率、任务成功率、审计覆盖率、P95”。

## 可选的经营分析描述

> 当前分析域以指标定义和事件汇聚边界为主，仓库明确要求不伪造经营数据。若你负责该模块，可使用下列“已实现范围”表述，不要写 GMV/转化提升。

- 设计内容效果、客服质量、销售能力、AI 用量与成本的统一分析边界，将指标口径、事实表和报表与交易/内容/模型等源系统解耦；所有分析结果作为可重建投影，不能反向覆盖业务事实。
- 建立跨域事件中必须携带 `trace_id`、可信 `tenant_id`、主体、店铺范围、用途、`event_id`、聚合 ID、版本和 Schema 版本的契约，为后续口径对账、质量监控和成本归因预留数据基础。

## 指标补齐清单

将以下真实数据填入对应 bullet，才能满足“问题/规模 + 动作/取舍 + 量化结果”的完整表达：

| 模块 | 需要补齐的真实数据 |
| --- | --- |
| 平台与多租户 | 租户数、活跃用户、权限策略数、跨租户拒绝测试覆盖、审计事件量、API P95。 |
| 直播切片 | 视频时长/文件大小、平均端到端耗时、ASR 费用、片段人工采纳率、导出成功率、失败重试成功率。 |
| 知识库/RAG | 文档/会话/chunk 数、冻结评测集规模、Recall@K、引用正确率、人工标注吞吐、回答 P95、单次成本。 |
| 销售考核 | 学员数、题库/知识版本数、AI 建议与人工终审偏差、复核耗时、申诉率。 |
| 内容运营 | Brief 数、生成任务数、审核通过率、从简报到发布耗时、平台发布成功率、素材复用率。 |
| 语音客服 | 会话数、首包/全程 P95、打断成功率、转人工率、转写准确率、RTC/模型成本。 |
| 模型治理 | 日调用量、模型数、Trace 完整率、结构化输出成功率、预算拦截率、故障降级成功率、灰度回滚时长。 |

## 不应写入简历的表述

- 不写“生产 P95 为 X”“Recall@5 从 X 提升到 Y”“成本降低 X%”，除非有可复现的监控或评测记录。
- 不将“依赖未配置时的演示/降级”写成“已接入真实向量库、RTC、对象存储或模型供应商”。
- 不把 837 个集成快照文件等同于“837 个功能”，也不把测试通过等同于生产规模。
- 不写“精通 Spring/React/FastAPI/MCP”；改为说明你在何种约束下做出的架构选择、失败处理和验证方式。

## 证据索引

- `电商AI智能运营平台-项目规划.md`：8 域的数据所有权、跨域调用与 Java/Python 边界。
- `docs/five-project-integration-architecture.md`：5 类来源能力的源码级审查、融合与风险控制。
- `docs/five-project-integration-verification.md`：837 文件快照、自动化测试、9 服务启动与安全验证。
- `backend/core-control-plane/README.md`、`backend/ai-business-control/README.md`：两个 Java 控制面的职责边界。
- `ai/runtime/README.md`、`ai/media-workers/README.md`：模型运行时、媒体 Worker 和受控任务协议。
- `frontend/tests/e2e/frontend.spec.ts`：主前端登录、工作区、媒体、内容运营、客服、销售知识、语音和治理回归。
